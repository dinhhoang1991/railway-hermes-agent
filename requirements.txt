deepseek-harness-sdk>=0.1.1rc1
python-dotenv>=1.0.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
pypdf>=4.0.0
# pymupdf>=1.24.0  # bỏ comment nếu cần nén PDF (scripts/compress_pdf.py)
# pytesseract>=0.3.10  # bỏ comment nếu cần OCR đọc PDF ảnh scan
#                      # (cần thêm: sudo apt install tesseract-ocr tesseract-ocr-vie)
